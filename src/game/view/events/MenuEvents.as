package game.view.events
{
	import utils.UintCounter;

	public class MenuEvents
	{
		public static const SHOW_WINDOW:	uint = UintCounter.getId();
		public static const HIDE_WINDOW:	uint = UintCounter.getId();
	}
}